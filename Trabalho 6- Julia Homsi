# Definição das variáveis
numeros = [10, 20, 30, 40]
frase = "Python é uma linguagem de programação poderosa."
palavra = "Python"

# Média aritmética
media = sum(numeros) / len(numeros)
print("Média aritmética dos números:", media)

# Quadrado de um dos números
num_quadrado = numeros[2]  # Escolhendo o terceiro número para calcular o quadrado
quadrado = num_quadrado ** 2
print("Quadrado de {}: {}".format(num_quadrado, quadrado))

# Quantidade de letras da palavra
quant_letras = len(palavra)
print("Quantidade de letras na palavra '{}': {}".format(palavra, quant_letras))

# Quantidade de espaços em branco na frase
quant_espacos = frase.count(" ")
print("Quantidade de espaços em branco na frase:", quant_espacos)

# Verificando se o primeiro número é maior que o segundo
primeiro_numero = numeros[0]
segundo_numero = numeros[1]
if primeiro_numero > segundo_numero:
    print("O primeiro número é maior que o segundo.")
else:
    print("O primeiro número não é maior que o segundo.")

# Encontrando o maior número
maior_numero = max(numeros)
print("O maior número é:", maior_numero)
